<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.roommade.domain.user.mapper.UserMapper">
    <select id="existsByEmail" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM users WHERE email = #{email})
    </select>

    <insert id="insertUser">
        INSERT INTO users (email, password_hash)
        VALUES (#{email}, #{passwordHash})
    </insert>

    <select id="findIdByEmail" resultType="long">
        SELECT id
        FROM users
        WHERE email = #{email}
    </select>

    <select id="findLoginUserByEmail"
            resultType="com.roommade.domain.user.dto.response.UserLoginSourceResponse">
        SELECT id, email, password_hash
        FROM users
        WHERE email = #{email}
    </select>

    <insert id="insertProfile">
        INSERT INTO user_profiles (
            user_id, name, birth_date, monthly_income, workplace_road_address,
            workplace_detail_address, deposit_limit, monthly_rent_limit
        ) VALUES (
            #{userId}, #{request.name}, #{request.birthDate}, #{request.monthlyIncome},
            #{request.workplaceRoadAddress}, #{request.workplaceDetailAddress},
            #{request.depositLimit}, #{request.monthlyRentLimit}
        )
    </insert>

    <insert id="insertCoinWallet">
        INSERT INTO coin_wallets (user_id)
        VALUES (#{userId})
    </insert>

    <insert id="insertIndependenceProgress">
        INSERT INTO independence_progress (user_id)
        VALUES (#{userId})
    </insert>
</mapper>
